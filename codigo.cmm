prototipo real soma(real, real);

semretorno principal(semparam) {
    real b;
    inteiro x, i;
    b = soma(1.2, 1.3);
    para (i = 0; i < 2; i = 1 + 2) {
            x = 3;
    }

}

real soma(real a, real c, real d) {
real b;
    retorne a + b;
}
