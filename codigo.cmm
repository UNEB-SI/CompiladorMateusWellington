prototipo inteiro soma(inteiro, inteiro);

semretorno principal(semparam) {
    inteiro b, x;
    b = soma(1, 2);

    se ((x < 1) || (x > 3) || (x > 4)) {

    } senao {

    }
}

inteiro soma(inteiro a, inteiro b) {
    retorne a + b;
}
